// Legacy root app splash screen placeholder.
// The real frontend app is in `frontend/src/components/SplashScreen.tsx`.
// This file is kept only so the `src/app` tree remains present, but it is not the active app.

interface SplashScreenProps {
  onIniciar: () => void;
}

export function SplashScreen({ onIniciar }: SplashScreenProps) {
  return null;
}
